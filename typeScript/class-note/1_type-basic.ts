var str = "hello";

const rim: string = "hello";
const num: number = 10;
const arr: Array<number> = [1, 2, 3];
const heroes: Array<string> = ["헐크", "송송"];

const items: number[] = [1, 2, 3];

//TS 튜플

let addressd: [string, number] = ["강남", 10];

let obj: object = {};

let persons: object = {
  name: "capt",
  age: 100,
};

let personsss: {} = {
  name: "capt",
  age: 100,
};

let persond: { name: string; age: number } = { name: "aa", age: 10 };

let show: boolean = true;
